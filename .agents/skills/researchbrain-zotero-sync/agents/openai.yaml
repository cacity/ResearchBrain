interface:
  display_name: "ResearchBrain Zotero Sync"
  short_description: "Incrementally sync Zotero metadata and local PDFs"
  default_prompt: "Use $researchbrain-zotero-sync to incrementally synchronize my Zotero library and verify every downstream job."
dependencies:
  tools:
    - type: "mcp"
      value: "researchbrain"
      description: "Local ResearchBrain Zotero mirror and job tools"
